story_id: e02s01
title: Produce Ubuntu 26.04 and Linux Mint 22.3 package artifacts
status: failing
bcps: 4
tasks:
  - id: 1
    description: Guard the two-target package workflow contract
    verify: tests/test-package-recipes.sh .
    risk: P1
    security: low
    status: passing
    allure:
      severity: high
      categories:
        - packaging
        - CI
  - id: 2
    description: Build and validate packages in pinned Mint and Ubuntu userspaces
    verify: tests/test-package-recipes.sh . && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/package-linux-mint.yml'))" && actionlint .github/workflows/package-linux-mint.yml && grep -q 'No new security findings in affected paths' specs/security/REVIEW.md
    risk: P0
    security: medium
    status: failing
    allure:
      severity: critical
      categories:
        - packaging
        - supply-chain
        - Security Review
  - id: 3
    description: Document both supported package targets and artifact names
    verify: grep -q 'Ubuntu 26.04' README.md && grep -q 'Ubuntu 26.04' docs/releases.md && grep -q 'Ubuntu 26.04' docs/architecture/build-and-test.md
    risk: P2
    security: none
    status: failing
    allure:
      severity: normal
      categories:
        - documentation
